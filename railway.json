{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "composer install --ignore-platform-reqs --no-scripts --optimize-autoloader --no-interaction"
    }
}
