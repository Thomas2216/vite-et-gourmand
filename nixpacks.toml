[phases.setup]
nixPkgs = ["php83", "php83Packages.composer", "php83Packages.imagick"]

[phases.install]
cmds = ["composer install --ignore-platform-req=ext-mongodb --optimize-autoloader --no-scripts --no-interaction"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public/"
